<nav class="navbar bg-success">
  <div class="container-fluid">
    <a class="navbar-brand text-white font-weight-bold">バスケ試合一覧</a>
    <div class="text-right">
    <%= link_to "バスケトップ", bsk_members_path, class: "btn btn-outline-light"  %>
    </div>
  </div>
</nav>


<% @matchs.each do |match|%>
<div class="card m-2 text-center border border-success border-2">
<h5 class="mt-2"><%=link_to match.created_at.in_time_zone("Tokyo").strftime("%Y年%m月%d日"), bsk_match_path(match) %></h5>
<%= button_to "削除", bsk_match_path(match), class: "btn btn-outline-danger btn-sm  mb-2", method: :delete %>
</div>
<% end %>
